// _obstacle.js
import { Drawable } from "./base_drawable.js";

export class Obstacle extends Drawable {

    constructor(scene){
        super(scene);
    }

    create(){
        super.create();
    }

    update(time, delta){
        super.update(time, delta);
    }

    dispose(){
        super.dispose();
    }
}